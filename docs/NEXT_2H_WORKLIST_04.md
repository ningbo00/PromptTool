# 下一轮 2 小时工作清单 04

执行原则：继续遵从 TDD 和 DDD。Phase 3 从动作构建逻辑抽离开始，先保证 AI 请求指令可测试，再逐步调整弹窗 UI。

## 目标

进入 Phase 3 实装：按 `AIOptimizeLayoutSpec` 定义的信息架构，先抽离 AI 优化弹窗的动作构建逻辑，再让 UI 使用该逻辑。

## 任务清单

### 1. 抽离 AI 动作构建

- 新增 AI 优化动作构建模块。
- 覆盖主流程：
  - 优化当前 Prompt。
  - 中文转英文 Prompt。
  - 生成 3 个变体。
- 覆盖高级工具：
  - AI 评分。
  - 提取关键词。
  - 推荐负面词。
  - 合规检验。

验收：

- 每个动作都有测试覆盖 messages 的 system/user 核心内容。
- 输出长度提示由动作构建模块统一处理。

### 2. AI 弹窗接入动作构建

- `_run_ai` 使用动作构建模块。
- `_zh_to_en` 使用动作构建模块。
- `_gen_variants` 使用动作构建模块。
- `_score` 使用动作构建模块。
- 本轮不改变网络请求和回调流程。

验收：

- 现有 AI 弹窗功能入口仍存在。
- `python -m pytest tests` 通过。

### 3. AI 弹窗第一屏信息架构重排

- 将顶部动作分成：
  - 主要流程。
  - 高级工具。
  - 结果操作。
- 先做布局分组，不大规模删除功能。

验收：

- 按钮不再全部堆在同一个视觉层级。
- 不改变内部业务行为。

## 暂不做

- 不改 `call_ai` 网络实现。
- 不做真实 API 请求测试。
- 不重构引导创作和描述转 Prompt 子弹窗。
